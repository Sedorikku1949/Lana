module.exports = {
    exe: async (message, args, modules, prefix, command) => {},
    config: {
        name: "gestion", aliases: [], category: "moderation", handler: { deleteInvoke: true, staff: true, dev: false, inPogress: true }, help: { desc: "...", use: "..." }, modules: ["fs"]
    }
  };

  // =xp add/remove user xp